// alert("Hello");
var message = "Hello";

var x = 27;
var temps = [14, 32, 36, 40];
var person = {
   first: "Nate", last: "White", address: {
      city: "Fort Collins", zip: 80521
   }
};

document.getElementById('user')
   <input id="user" type="text">
user = document.getElementById('user')
console.log(temps[2]);

console.log(person.address.city  + " " +  person.address.zip);

if (person.address.zip === 80521) {
   console.log("Hello " + person.first );
} else {
   console.log("What are you doing here " + person.first);
}

document.body.offsetHeight;


switch (age) {
   case 21:
            console.log("I'm 21");
            break;
   case 30:
            console.log("I'm 30");
            break
   default:
            console.log("I'm " + age);
            break;
}
// can use switch as a parser
